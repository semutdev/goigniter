# Application
APP_ENV=local
APP_PORT=:8080
APP_URL=http://localhost:8080
APP_KEY={{.AppKey}}

# Database
DB_DRIVER={{.DbType}}
DB_DSN={{.DbDsn}}

# Seeder (set to true once to create tables and admin user)
DB_SEED=false